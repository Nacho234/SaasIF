# Modo de la app: "demo" (datos falsos locales) o "prod" (backend real).
# Por defecto es "demo". El modo "prod" todavía no está conectado (ver docs/HANDOFF).
VITE_APP_MODE=demo

# --- Producción (Supabase) — completar en fases posteriores, todavía sin usar ---
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
