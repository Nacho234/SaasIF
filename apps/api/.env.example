# Base de datos PostgreSQL (Neon, Supabase, Railway o local).
DATABASE_URL="postgresql://user:password@localhost:5432/mostrador?schema=public"

# Auth
JWT_SECRET="cambiar-esto-por-un-secreto-largo-y-aleatorio"
JWT_EXPIRES_IN="7d"

# Servidor
PORT=3000
# Orígenes permitidos para CORS (separados por coma). En dev, el frontend Vite.
CORS_ORIGINS="http://localhost:5173,http://localhost:5174"
